---
phase: 01-appointment-history-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/
  - lib/appointments.ts
autonomous: false
requirements: [APPT-01, APPT-03]
must_haves:
  truths:
    - "An `appointments` table exists in the database with date, optional service, optional source, scoped by business and customer."
    - "Every existing customer with a non-null last_appointment_at has exactly one backfilled appointment row whose date equals that value."
    - "The D-04 cache invariant holds after the migration: customers.last_appointment_at == MAX(appointments.date) for that customer, NULL when none."
    - "Calling recomputeLastAppointment(customerId, businessId) re-derives the cache to MAX(appointment.date) or null, scoped by business."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Appointment model + Customer.appointments + Business.appointments back-relations"
      contains: "model Appointment"
    - path: "lib/appointments.ts"
      provides: "recomputeLastAppointment shared cache helper"
      exports: ["recomputeLastAppointment"]
    - path: "prisma/migrations/"
      provides: "CreateTable appointments + indexes + cascade FKs + appended D-06 raw-SQL backfill"
      contains: "INSERT INTO \"appointments\""
  key_links:
    - from: "lib/appointments.ts"
      to: "prisma.customer.updateMany"
      via: "updateMany with businessId in where (ownership re-check)"
      pattern: "updateMany"
    - from: "migration.sql backfill"
      to: "customers.last_appointment_at"
      via: "INSERT … SELECT … WHERE last_appointment_at IS NOT NULL"
      pattern: "WHERE c\\.\"last_appointment_at\" IS NOT NULL"
---

<objective>
Lay the shared FOUNDATION every visit-history slice builds on: a real `Appointment`
model behind the existing `Customer.lastAppointmentAt` field, the one migration that
creates the table AND backfills legacy rows (D-06) so the D-04 cache invariant holds
from day one, and the single `recomputeLastAppointment` helper that all five mutation
paths will call so the cache can never drift.

Purpose: APPT-01 needs per-visit storage; APPT-03 needs `lastAppointmentAt` preserved
as a derived cache so `rebooking.ts` (untouched, D-05) keeps firing correctly. This is
the regression boundary — the whole phase rests on the invariant established here.
Output: `appointments` table (migrated + backfilled), `lib/appointments.ts` helper.

This plan is NOT autonomous — the [BLOCKING] migration step (`prisma migrate dev`)
may surface an interactive shadow-DB / drift prompt that cannot be suppressed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-appointment-history-foundation/01-CONTEXT.md
@.planning/phases/01-appointment-history-foundation/01-RESEARCH.md
@.planning/phases/01-appointment-history-foundation/01-PATTERNS.md
@.planning/phases/01-appointment-history-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1-01-01: Add Appointment model + back-relations to schema</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma — `Customer` model L58-77 (Timestamptz/@map/Cascade conventions, the `lastAppointmentAt` field to keep), `Business` model L29-55 (back-relation list to extend), `Message` L130+ / `Feedback` L196+ (the `onDelete: SetNull` log convention we deliberately DIFFER from)
    - .planning/phases/01-appointment-history-foundation/01-PATTERNS.md §"prisma/schema.prisma — add Appointment model" (the exact model block + onDelete decision rationale)
    - .planning/phases/01-appointment-history-foundation/01-RESEARCH.md §"Pattern 1" (onDelete decision: Cascade + non-null customerId, confirmed against schema evidence)
  </read_first>
  <action>
    Add a new `model Appointment` to prisma/schema.prisma per D-01/D-02. Fields: `id String @id @default(uuid()) @db.Uuid`; `businessId String @map("business_id") @db.Uuid`; `customerId String @map("customer_id") @db.Uuid` (NON-NULL — deliberately unlike the nullable `Message`/`Feedback` `customerId` SetNull logs); `date DateTime @db.Timestamptz(6)` (D-03: store the full timestamp even though the add/import UI only captures a date-only input — time optional; Phase 2 cadence math works off the date); `service String?` (optional free-text, NOT a structured catalog); `source String?` (nullable plain string seeded with 'manual'/'csv'/'backfill', left open so a future integration writes its own provider name without a migration); `createdAt DateTime @default(now()) @map("created_at") @db.Timestamptz(6)`. Relations: `business Business @relation(fields: [businessId], references: [id], onDelete: Cascade)` and `customer Customer @relation(fields: [customerId], references: [id], onDelete: Cascade)` — Cascade because appointments ARE the customer's history and are meaningless without them (D-01, matches the Cascade family, differs from the SetNull logs). Indexes: `@@index([businessId])` and `@@index([customerId, date])` (newest-visit-per-customer + the rebooking range query, D-02). `@@map("appointments")`. Then add the required Prisma back-relations: `appointments Appointment[]` to `model Customer` (alongside `messages`/`feedback`) and `appointments Appointment[]` to `model Business` (alongside its relation list). Do NOT remove or change `Customer.lastAppointmentAt` — it stays as the derived cache (D-04). Do NOT touch any enum or other model.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `prisma/schema.prisma` contains `model Appointment` with `@@map("appointments")`.
    - `Appointment.customerId` is non-null `String @map("customer_id")` and the customer relation uses `onDelete: Cascade`.
    - `Appointment` declares `@@index([customerId, date])`.
    - `model Customer` and `model Business` each declare an `appointments Appointment[]` back-relation.
    - `Customer.lastAppointmentAt` is unchanged.
    - `npx tsc --noEmit` exits 0 (after Task 1-01-02 regenerates the client; on schema-only edit, allow Prisma-format check via `npx prisma format` exiting 0 if types not yet generated).
  </acceptance_criteria>
  <done>Schema declares the Appointment model with Cascade FKs, the composite index, and both back-relations; lastAppointmentAt preserved.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1-01-02: [BLOCKING] Generate migration, append D-06 backfill SQL, apply, restart dev</name>
  <files>prisma/migrations/</files>
  <read_first>
    - prisma/migrations/20260603002443_add_feedback/migration.sql — the raw-SQL DDL template (UUID PK, TIMESTAMPTZ(6), CREATE INDEX, ADD CONSTRAINT … ON DELETE …) the generated SQL will mirror
    - .planning/phases/01-appointment-history-foundation/01-PATTERNS.md §"prisma/migrations/<ts>_add_appointment_model/migration.sql" (the appended D-06 backfill block, verbatim)
    - .planning/phases/01-appointment-history-foundation/01-RESEARCH.md §"Pattern 1" + §"Pitfall 2" (the `.env.local` workflow correction; A1 gen_random_uuid availability)
    - AGENTS.md §"DB migrations" — but note RESEARCH overrides it: `npm run db:migrate` reads `.env.local`, NOT `.env`.
  </read_first>
  <action>
    Create AND apply the migration in the create-only-then-edit sequence so the D-06 backfill ships in the SAME migration (D-06):
    1. Run `npm run db:migrate -- --name add_appointment_model --create-only` (the npm script wraps Prisma in `dotenv -e .env.local`; this generates the migration folder + `migration.sql` WITHOUT applying). If `--create-only` is not exposed by the wrapper, run `npx dotenv -e .env.local -- prisma migrate dev --name add_appointment_model --create-only`.
    2. Open the generated `prisma/migrations/<timestamp>_add_appointment_model/migration.sql`. Confirm it contains `CREATE TABLE "appointments"` with UUID PK, `TIMESTAMPTZ(6)` columns, `CREATE INDEX` for both indexes, and two `ADD CONSTRAINT … FOREIGN KEY … ON DELETE CASCADE ON UPDATE CASCADE` (business_id → businesses, customer_id → customers). The customer FK MUST be `ON DELETE CASCADE` (NOT SetNull).
    3. Append the D-06 backfill block at the END of `migration.sql` (after CreateTable/indexes/FKs): `INSERT INTO "appointments" ("id", "business_id", "customer_id", "date", "service", "source", "created_at") SELECT gen_random_uuid(), c."business_id", c."id", c."last_appointment_at", NULL, 'backfill', CURRENT_TIMESTAMP FROM "customers" c WHERE c."last_appointment_at" IS NOT NULL;` — preserve the exact column order and the `WHERE … IS NOT NULL` guard.
    4. Apply it: `npm run db:migrate -- --name add_appointment_model` (re-running migrate dev applies the pending edited migration and runs `prisma generate`). If migrate dev reprompts for a name, the migration already exists from step 1 — let it apply the pending one.
    5. Restart `npm run dev` so the regenerated `@prisma/client` `Appointment` type loads (AGENTS.md).
    PAUSE for the human if the migrate command surfaces an interactive shadow-database, drift, or reset prompt (cannot be auto-suppressed) — the human confirms the prompt and that the migration applied to the correct Supabase DB. Do NOT run bare `prisma migrate` (it reads `.env`, the wrong env file — Pitfall 2). If `gen_random_uuid()` errors as unavailable (A1), fall back to `uuid_generate_v4()` or report it.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - A `prisma/migrations/<ts>_add_appointment_model/migration.sql` exists containing `CREATE TABLE "appointments"` and an appended `INSERT INTO "appointments" … SELECT … FROM "customers" c WHERE c."last_appointment_at" IS NOT NULL`.
    - The customer FK constraint reads `ON DELETE CASCADE` (not SET NULL).
    - The migration is APPLIED to the live Supabase DB: `npm run db:studio` shows an `appointments` table; every customer with a non-null `last_appointment_at` has exactly one backfilled row (source='backfill').
    - Backfill invariant assertion returns 0 rows: `SELECT count(*) FROM customers c WHERE c.last_appointment_at IS NOT NULL AND NOT EXISTS (SELECT 1 FROM appointments a WHERE a.customer_id=c.id)` → 0.
    - The global cache-invariant SQL (01-VALIDATION.md) returns 0 rows post-backfill.
    - `npx tsc --noEmit` exits 0 (generated client now includes `prisma.appointment`).
  </acceptance_criteria>
  <done>Migration applied with backfill; appointments table populated for legacy customers; D-04 invariant holds; Prisma client regenerated and dev restarted.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 1-01-03: Create recomputeLastAppointment shared cache helper</name>
  <files>lib/appointments.ts</files>
  <read_first>
    - lib/appointments.ts — does not exist yet (NEW file)
    - app/(app)/customers/actions.ts L122-132 (`deleteCustomerAction`) — the ownership-safe `deleteMany({ where: { id, businessId } })` discipline to mirror with `updateMany`
    - lib/prisma.ts (import path `@/lib/prisma`) — the shared client this helper queries
    - .planning/phases/01-appointment-history-foundation/01-PATTERNS.md §"lib/appointments.ts — recomputeLastAppointment" (the exact helper body + the five write-sites it serves)
    - .planning/phases/01-appointment-history-foundation/01-RESEARCH.md §"Pattern 2" + §"Pitfall 1" (one helper, all paths; bulk = once per distinct customer)
  </read_first>
  <action>
    Create `lib/appointments.ts` exporting one async function `recomputeLastAppointment(customerId: string, businessId: string): Promise<void>` per D-04. Implementation: `findFirst` the customer's newest appointment scoped by `{ customerId, businessId }`, `orderBy: { date: "desc" }`, `select: { date: true }`; then `prisma.customer.updateMany({ where: { id: customerId, businessId }, data: { lastAppointmentAt: latest?.date ?? null } })`. Use `updateMany` (NOT `update`) so `businessId` is in the `where` — same ownership re-check as `deleteCustomerAction` (a crafted call can't touch another business's customer). Set the cache to `null` when the customer has no appointments. Import `prisma` from `@/lib/prisma`. Add a short doc comment stating the D-04 invariant (`lastAppointmentAt == MAX(appointment.date)`, null when none) and that ALL five mutation paths must call this. Do NOT add a per-row variant — bulk callers (CSV/customer import) recompute once per distinct customerId after the batch (Pitfall 1). Do NOT touch `lib/automations/rebooking.ts` (D-05).
  </action>
  <verify>
    <automated>npx tsc --noEmit; npx eslint lib/appointments.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/appointments.ts` exports `recomputeLastAppointment(customerId, businessId)`.
    - The customer write uses `prisma.customer.updateMany` with `businessId` in the `where` (grep `updateMany` present; grep `prisma.customer.update(` ABSENT — `grep -v '^//' lib/appointments.ts` shows no singular `.update(`).
    - The newest-appointment lookup is scoped by both `customerId` AND `businessId`.
    - Cache is set to `null` when no appointment exists (data uses `latest?.date ?? null`).
    - `npx tsc --noEmit` exits 0; `npx eslint lib/appointments.ts` exits 0.
  </acceptance_criteria>
  <done>One scoped, ownership-safe helper exists that re-derives lastAppointmentAt to MAX(appointment.date) or null; ready for all five mutation paths to call.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration SQL → production Supabase Postgres | DDL + data backfill run against the live DB; wrong env file targets the wrong/empty DB |
| `recomputeLastAppointment(customerId, businessId)` caller → DB | helper trusts its two args; callers (next plans) are responsible for passing a business-scoped customerId |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Backfill `INSERT … SELECT` writing cross-business rows | mitigate | SELECT copies `c.business_id` from each customer's own row — backfill cannot mix businesses; FK enforces referential integrity |
| T-01-02 | Elevation of Privilege | `recomputeLastAppointment` updating another business's customer | mitigate | `updateMany` with `businessId` in the `where` (ownership re-check, mirrors `deleteCustomerAction`); never singular `update` |
| T-01-03 | Denial of Service | Bare `prisma migrate` against `.env` (wrong DB) corrupts/empties data | mitigate | Use `npm run db:migrate` (wraps `dotenv -e .env.local`); blocking human checkpoint confirms the prompt + correct DB before apply (Pitfall 2) |
| T-01-04 | Information Disclosure | Migration A1 (`gen_random_uuid()`) unavailable → partial/failed backfill leaves stale cache | accept→mitigate | Supabase ships pgcrypto; checkpoint verifies backfill row counts + invariant SQL = 0 before proceeding; fallback `uuid_generate_v4()` |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase — RESEARCH §Package Legitimacy Audit: N/A (all deps already pinned in package.json) |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0; `npx eslint lib/appointments.ts` exits 0.
- `npm run db:studio` → `appointments` table exists with the D-02 columns + indexes.
- Backfill assertion = 0: `SELECT count(*) FROM customers c WHERE c.last_appointment_at IS NOT NULL AND NOT EXISTS (SELECT 1 FROM appointments a WHERE a.customer_id=c.id)`.
- Global cache invariant SQL (01-VALIDATION.md §"Cache invariant") returns 0 rows.
- Regression sanity: the dashboard "Test my setup" rebooking preview returns the same due-list as before the migration for unchanged data (rebooking.ts reads the now-backfilled cache, unchanged — D-05/APPT-03).
- Maps to 01-VALIDATION.md rows: APPT-01 (table exists), APPT-03 (backfill), APPT-03 (rebooking regression).
</verification>

<success_criteria>
- `appointments` table exists, migrated + backfilled; every legacy customer with a last visit has one seed row.
- `Customer.lastAppointmentAt` preserved as the derived cache; D-04 invariant SQL returns 0 rows.
- `recomputeLastAppointment(customerId, businessId)` exists, scoped, ownership-safe (`updateMany`).
- `rebooking.ts` and `send-message.ts` untouched; rebooking preview unchanged for unchanged data.
</success_criteria>

<output>
Create `.planning/phases/01-appointment-history-foundation/01-01-SUMMARY.md` when done.
</output>
