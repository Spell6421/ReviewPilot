---
phase: 01-appointment-history-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - app/(app)/customers/actions.ts
requirements: [APPT-02, APPT-03]
autonomous: true
must_haves:
  truths:
    - "Adding a customer with a last-appointment date creates a backing Appointment (source='manual') instead of writing lastAppointmentAt directly, and the derived cache matches it."
    - "Importing customers from CSV with last_appointment_at values creates a backing Appointment (source='csv') per such row, and each customer's cache matches its seed visit."
    - "After customer create/import, no customer has a non-null last_appointment_at without a matching appointment row (no orphan)."
    - "The D-04 cache invariant holds after customer create and after customer import."
  artifacts:
    - path: "app/(app)/customers/actions.ts"
      provides: "createCustomerAction + importCustomersAction now seed a backing appointment + recompute (D-07)"
      contains: "recomputeLastAppointment"
  key_links:
    - from: "app/(app)/customers/actions.ts"
      to: "lib/appointments.ts#recomputeLastAppointment"
      via: "called after seeding the backing appointment(s)"
      pattern: "recomputeLastAppointment"
    - from: "createCustomerAction"
      to: "prisma.appointment.create"
      via: "seed appointment when a last-visit value is provided"
      pattern: "prisma.appointment.create"
---

<objective>
Close the last two of the FIVE cache-mutation paths (D-04/D-07): the existing
customer create and customer-CSV import flows. Today both write `Customer.lastAppointmentAt`
directly; after this plan they instead seed a backing `Appointment` (source 'manual'/'csv')
and recompute the cache — so a last-visit value can never be orphaned and the invariant
holds across every entry point.

Purpose: APPT-02 (history seeded via the existing customer flows too) and APPT-03 (the
cache stays derived and correct, no orphans). This plan touches only
`app/(app)/customers/actions.ts`, so it runs in parallel with Plan 02 in Wave 2.
Output: edited `createCustomerAction` + `importCustomersAction`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-appointment-history-foundation/01-CONTEXT.md
@.planning/phases/01-appointment-history-foundation/01-RESEARCH.md
@.planning/phases/01-appointment-history-foundation/01-PATTERNS.md
@.planning/phases/01-appointment-history-foundation/01-VALIDATION.md
@.planning/phases/01-appointment-history-foundation/01-01-SUMMARY.md
@lib/appointments.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1-03-01: createCustomerAction seeds a backing appointment (D-07)</name>
  <files>app/(app)/customers/actions.ts</files>
  <behavior>
    - Add a customer WITH a last-appointment date → customer row created, ONE appointment row (source='manual', service=null, date=that value), and customers.last_appointment_at equals the seed date (recomputed, not written directly).
    - Add a customer WITHOUT a last-appointment date → customer row created, NO appointment row, last_appointment_at stays null.
    - No orphan: there is never a non-null last_appointment_at without a matching appointment row.
  </behavior>
  <read_first>
    - app/(app)/customers/actions.ts — `createCustomerAction` L20-66 (current direct `lastAppointmentAt` write at L54-62 to replace; date parse L45-52 to keep)
    - lib/appointments.ts — `recomputeLastAppointment` signature
    - .planning/phases/01-appointment-history-foundation/01-PATTERNS.md §"app/(app)/customers/actions.ts (EDIT, D-07)" + §"lib/appointments.ts" write-sites list
    - .planning/phases/01-appointment-history-foundation/01-RESEARCH.md §"Code Examples" D-07 block + §"Pitfall 1"
  </read_first>
  <action>
    Edit `createCustomerAction` (D-07): keep all existing validation (name required, phone normalization, phone-or-email rule, date parse). Change the create so it no longer writes `lastAppointmentAt` directly. Instead: create the customer WITHOUT a `lastAppointmentAt` field; if a valid `lastAppointmentAt` date was provided, `prisma.appointment.create({ data: { businessId: business.id, customerId: <new customer id>, date: <parsed date>, service: null, source: "manual" } })`; then `await recomputeLastAppointment(newCustomer.id, business.id)` so the cache is derived (capture the created customer's id from the `create` return). Keep `revalidatePath("/customers")` and the `{ successAt }` return. Import `recomputeLastAppointment` from `@/lib/appointments`. Do NOT change `deleteCustomerAction` or the form-state types. Do NOT touch `rebooking.ts`.
  </action>
  <verify>
    <automated>npx tsc --noEmit; npx eslint "app/(app)/customers/actions.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `createCustomerAction` no longer passes `lastAppointmentAt` to `prisma.customer.create` (grep the `customer.create` data block — `lastAppointmentAt` absent there).
    - When a date is provided it calls `prisma.appointment.create` with `source: "manual"` then `recomputeLastAppointment(...)`.
    - Behavior (DB): add a customer with a last-visit date → exactly one `appointments` row (source='manual') AND `customers.last_appointment_at` equals it; add without a date → zero appointment rows, cache null.
    - `npx tsc --noEmit` exits 0; eslint on the file exits 0.
  </acceptance_criteria>
  <done>createCustomerAction seeds a backing appointment and derives the cache; no orphaned last-visit value possible from the add-customer form.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1-03-02: importCustomersAction seeds backing appointments + single recompute pass (D-07)</name>
  <files>app/(app)/customers/actions.ts</files>
  <behavior>
    - Import a CSV where some rows have last_appointment_at → each such row's customer gets ONE backing appointment (source='csv', date=that value); each affected customer's cache equals its seed date.
    - Rows without a last_appointment_at → customer created, no appointment, cache null.
    - Recompute runs once per distinct newly-affected customer (not per row), and the invariant holds for every imported customer.
  </behavior>
  <read_first>
    - app/(app)/customers/actions.ts — `importCustomersAction` L80-116 (current `createMany` writing `lastAppointmentAt` at L100-108 to replace; valid-row filter L95-98; the `{ imported, skipped, successAt }` return)
    - lib/appointments.ts — `recomputeLastAppointment` (bulk rule: once per distinct customerId)
    - .planning/phases/01-appointment-history-foundation/01-PATTERNS.md §"app/(app)/customers/actions.ts (EDIT, D-07)" (bulk path: createMany customers, createMany appointments, one recompute per distinct new customerId)
    - .planning/phases/01-appointment-history-foundation/01-RESEARCH.md §"Open Questions" Q2 (transaction boundary — recompute is idempotent; tx is nice-to-have) + §"Pitfall 1"
  </read_first>
  <action>
    Edit `importCustomersAction` (D-07): keep the server re-parse (`parseCustomersCsv`), the fatal check, and the valid-row filter. Stop writing `lastAppointmentAt` in the customer `createMany`. New flow: (1) `createMany` the customers WITHOUT `lastAppointmentAt`; because `createMany` does not return ids, re-fetch the just-created customers scoped to the business to map each valid row to its new customer id (e.g. match by normalized phone/email, or insert per-row with `create` capturing ids if matching is ambiguous — choose the approach that reliably maps a row to exactly one new customer id without cross-row collision; prefer per-row `prisma.appointment.create` keyed off captured ids over a fragile re-fetch). (2) For each valid row that carried a `lastAppointmentAt`, `prisma.appointment.create({ data: { businessId: business.id, customerId, date: r.lastAppointmentAt, service: null, source: "csv" } })`. (3) Collect the DISTINCT affected customerIds and call `recomputeLastAppointment(id, business.id)` ONCE per distinct id (never per row — Pitfall 1). Wrapping the inserts in `prisma.$transaction` is optional (recompute is idempotent, Q2). Keep the `{ imported, skipped, successAt }` return shape and `revalidatePath("/customers")`. Do NOT change the customer CSV parser or `parseCustomersCsv` — D-07 keeps the existing `last_appointment_at` CSV column; it now seeds an appointment rather than a bare cache value. Import `recomputeLastAppointment`.
  </action>
  <verify>
    <automated>npx tsc --noEmit; npx eslint "app/(app)/customers/actions.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `importCustomersAction` no longer writes `lastAppointmentAt` in the customer insert; rows with a last-visit value produce a `prisma.appointment.create`/insert with `source: "csv"`.
    - `recomputeLastAppointment` is called once per distinct affected customer (not inside a per-row create loop without dedup).
    - Behavior (DB): import a CSV with some `last_appointment_at` values → one `appointments` row (source='csv') per such customer AND each cache equals its seed; rows without a value → no appointment, cache null.
    - Global cache invariant SQL returns 0 rows after import.
    - `npx tsc --noEmit` exits 0; eslint on the file exits 0.
  </acceptance_criteria>
  <done>Customer CSV import seeds backing appointments and recomputes each affected customer's cache once; no orphaned last-visit values; invariant holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → createCustomerAction / importCustomersAction (POST) | Server Actions are POST-reachable; name/phone/email/date and uploaded CSV are untrusted |
| Uploaded CSV → server re-parse | client preview is advisory; the server re-parse is authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | Unauthenticated POST to create/import | mitigate | existing `requireCurrentBusiness()` retained at the top of both actions (auth inside the action) |
| T-03-02 | Elevation of Privilege | Seeded appointment attributed to another business | mitigate | every `appointment.create` uses `businessId: business.id` and a customerId created within this business in the same action |
| T-03-03 | Tampering | Untrusted CSV last_appointment_at / phone producing bad rows | mitigate | server re-parse via `parseCustomersCsv` (existing per-row validation, E.164 normalization) before any insert; invalid rows excluded |
| T-03-04 | Tampering | Cache drift if a path forgets to recompute (silent APPT-03 break) | mitigate | both edited paths call the single `recomputeLastAppointment`; invariant SQL = 0 gate (Pitfall 1) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No packages installed — RESEARCH §Package Legitimacy Audit: N/A |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0; `npx eslint "app/(app)/customers/actions.ts"` clean.
- D-07 (01-VALIDATION.md): add a customer with a last-visit date → backing `appointments` row (source manual/csv) exists AND `last_appointment_at` matches (no orphan).
- Global cache invariant SQL returns 0 rows after a create and after an import.
- Regression: existing customer-CSV import still reports `{ imported, skipped }` and still skips invalid rows.
- Maps to 01-VALIDATION.md rows: D-07 (no orphan), APPT-03 (backfill/invariant family), APPT-02 (CSV seed via customer flow).
</verification>

<success_criteria>
- `createCustomerAction` and `importCustomersAction` seed a backing appointment + recompute instead of writing the cache directly.
- No customer ends up with a non-null `last_appointment_at` lacking a matching appointment row.
- Invariant SQL returns 0 after both flows; existing import counts/skip behavior preserved.
- `rebooking.ts`/`send-message.ts` untouched.
</success_criteria>

<output>
Create `.planning/phases/01-appointment-history-foundation/01-03-SUMMARY.md` when done.
</output>
